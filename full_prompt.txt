{"step_index":154,"source":"USER_EXPLICIT","type":"USER_INPUT","status":"DONE","created_at":"2026-09-20T09:31:48Z","content":"<USER_REQUEST>\nENVIRONMENT: Windows PowerShell. Use `python -m pip`, `python -m pyflakes`, and `Select-String` (or `rg`) instead of `grep`. Use `python -m venv .venv` and `.\\.venv\\Scripts\\Activate.ps1`.\r\n\r\nROLE: You are working on SkillBank, a React 18 (Vite) + FastAPI + MongoDB project for Smart India Hackathon 2026 (MoSPI, PS 26101: AI learning platform for competency gaps, iGOT-based recommendations, and MCQ generation). An audit found verified security and correctness bugs. Fix ONLY the tasks below. Do not add features, redesign UI, or refactor anything not listed.\r\n\r\nRULES\r\n- Create git branch `day1-fixes` first; commit after each numbered task.\r\n- NEVER open, print, log, or edit any `.env` file. Only edit `.env.example` files. The user has already added GOOGLE_CLIENT_ID and SESSION_SECRET to backend/.env.\r\n- Tests must NOT read .env. Set test values for GOOGLE_CLIENT_ID and SESSION_SECRET via a conftest.py or monkeypatch, and mock Google verification.\r\n- Read each file fully before changing it. Keep the existing code style.\r\n- If a task's premise does not match the code, stop and tell me instead of guessing.\r\n- Run everything in VERIFICATION and report real output. Never claim something works without running it.\r\n\r\nWORK IN THIS ORDER (P0 security first): 2, 3, 4, 5, 6, then 1, 7, 8, 9, 10, 11, 12.\r\n\r\nTASK 1: Dependencies (backend/requirements.txt)\r\n- gemini_service.py line 6 does `from google import genai`, but requirements.txt lists `google-generativeai`, which does not provide it. Replace with `google-genai`.\r\n- Add explicit `google-auth`, `requests`, `PyJWT`.\r\n- Confirm in a fresh venv that `from google import genai` imports.\r\n\r\nTASK 2: Real authentication (backend/routes/auth.py, backend/dependencies.py, backend/config.py, backend/.env.example)\r\n- In auth.py delete `_decode_jwt_payload_fallback` and the whole `except` fallback branch (~lines 76-92). If `verify_oauth2_token` fails for any reason, return HTTP 401 with the generic message \"Invalid Google credential\" (no exception text).\r\n- Require GOOGLE_CLIENT_ID: if empty, the app must fail at startup with a clear error. Never call verify with `audience=None`.\r\n- Replace token format `google_token_<user_id>` with a signed JWT (HS256, PyJWT). Claims: sub=user_id, iat, exp (8 hours). Secret from a new `SESSION_SECRET` setting in config.py; startup must fail if it is empty or under 32 characters.\r\n- dependencies.py `get_current_user`: verify signature and expiry, then load the user by `sub`. Invalid or expired returns 401. `require_admin` must read the role from the database document, not from the token.\r\n- Add `GOOGLE_CLIENT_ID=` and `SESSION_SECRET=` (with a comment on generating one) to backend/.env.example.\r\n\r\nTASK 3: Authorization on every route\r\n- Add `Depends(get_current_user)` to all routes in users.py, skills.py, quiz.py, market.py.\r\n- courses.py: `GET /courses` needs auth; `POST /courses/seed` uses `require_admin`.\r\n- users.py: replace `GET /users/{user_id}` with `GET /users/me`. Remove lookup by email or name. Change `POST /users/profile` to `PUT /users/me`, which updates only the caller's own document and never matches by name or email.\r\n- skills.py `/analyze`: ignore any `user_id` in the body; use the authenticated user's id for reading and persisting. Stop overwriting `current_skills` with only matched skills; persist what the user submitted.\r\n- Only `/api/auth/*`, `/`, and `/api/health` may stay public.\r\n\r\nTASK 4: Role escalation\r\n- Remove `role` from UserProfileBase, UserProfileCreate, and UserProfileUpdate in models/user.py. New users always get \"learner\".\r\n- No update path may write `role`, `_id`, `id`, `email`, or `google_id` from client input. Use an explicit allow-list of updatable fields.\r\n- Rewrite set_admin.py to take the email from `sys.argv[1]` (no hardcoded address). It is the only way to grant admin.\r\n\r\nTASK 5: CORS (backend/main.py, backend/config.py)\r\n- Remove `\"*\"` from CORS_ORIGINS in config.py.\r\n- main.py must use `settings.CORS_ORIGINS` for `allow_origins` instead of `[\"*\"]`. Allow extra origins from an optional comma-separated `CORS_ORIGINS` env var.\r\n\r\nTASK 6: Frontend authentication (src/context/AuthContext.jsx and callers)\r\n- Delete the local JWT decode fallback (~lines 64-83). If the backend rejects or is unreachable, show the error and do NOT log the user in.\r\n- Create one helper (e.g. src/utils/apiFetch.js) that adds `Authorization: Bearer <token>` and logs the user out on a 401. Use it for every backend call: skills/analyze, skills/extract-resume, quiz/generate-topic, market/demand, admin/overview.\r\n- Remove `user_id` from the /skills/analyze request body in SkillGapAnalyzer.jsx.\r\n- Make src/config/api.js the single source: `import.meta.env.VITE_API_BASE_URL` with default `http://localhost:8000/api`. Delete the duplicate constant in AdminDashboard.jsx and import the shared one.\r\n\r\nTASK 7: Broken code in backend/routes/skills.py\r\n- `logger` and `re` are used (~lines 114, 116, 123, 141) but never imported or defined. Add `import re`, `import logging`, and a module logger.\r\n- Remove every hardcoded fake skill list returned as if extracted from a resume (React, TypeScript, Node.js, Docker...) in skills.py and in gemini_service.py `extract_skills_from_resume_text`. If nothing can be extracted, return an empty list plus `\"engine\": \"fallback\"` and a `\"warning\"` message, and have the frontend display it.\r\n\r\nTASK 8: Stop fallbacks masquerading as real results (quiz.py, gemini_service.py)\r\n- `generate_mcqs_from_text` returns 5 canned unrelated questions when Gemini is off or fails. Change it to raise, and have quiz.py return HTTP 503 \"Quiz generation unavailable\".\r\n- quiz.py `source_model` must reflect what actually produced the questions, not merely whether a key exists.\r\n- `generate_mcqs_for_topic`'s templated fallback may stay, but the response must set `\"engine\": \"template_fallback\"` so the UI can label it.\r\n\r\nTASK 9: Scoring bug (backend/services/skill_service.py)\r\n- SKILL_ALIASES maps \"sampling\" to \"National Sampling (NSSO)\", so a user who lists \"Sampling\" can never satisfy the Statistical Officer requirement \"Sampling\". Fix normalization so an alias resolves to the canonical name that exists in the target role's required_skills, and make \"Sampling\" match \"Sampling\" for that role.\r\n\r\nTASK 10: Upload and error hardening\r\n- skills.py `/extract-resume` and quiz.py `/generate`: reject files over 5 MB with HTTP 413; read in a bounded way; verify a .pdf begins with `%PDF`.\r\n- Replace every `detail=f\"...{str(e)}\"` that leaks exception text (quiz.py x2, market.py, admin.py, auth.py) with a generic message; log the real exception server-side.\r\n\r\nTASK 11: Honesty fixes\r\n- src/components/Dashboard.jsx (iGOT Karmayogi Sync block, ~lines 207-219): remove the fake \"Sync Credentials\" success toast and the text \"Connected with DigiLocker and National Career Service (NCS)\". Replace with a truthful line such as \"iGOT integration: demo catalogue\".\r\n- market_service.py: `is_live_source` must be False unless a real HTTP call to a live provider exists. None does, so set False and label the source \"curated sample data\".\r\n\r\nTASK 12: Tests (backend/tests/test_security.py with pytest + httpx ASGITransport)\r\nProve that:\r\n1. A forged unsigned JWT sent to /api/auth/google returns 401.\r\n2. Every protected route returns 401 without a token.\r\n3. A learner token on /api/admin/overview returns 403.\r\n4. Sending `role: \"admin\"` in a profile update does not change the role.\r\n5. A user cannot read or modify another user's data.\r\n6. A hand-written `google_token_<id>` is rejected.\r\n7. An expired token is rejected.\r\n8. CORS does not reflect an unlisted Origin.\r\n9. Skill \"Sampling\" now satisfies requirement \"Sampling\".\r\nUpdate the existing 10 tests in backend/test_backend.py so they authenticate (helper that creates a user and mints a valid session token).\r\n\r\nOUT OF SCOPE: new features, iGOT/LLM/scoring redesign, UI restyling, deleting scratch/ or index-legacy.html.\r\n\r\nVERIFICATION (report real output)\r\n- Fresh venv: `python -m pip install -r backend/requirements.txt`, then `python -c \"from google import genai\"`.\r\n- `python -m compileall backend` and `python -m pyflakes backend` (no undefined names).\r\n- `pytest` passes, including all new tests.\r\n- `npm run build` succeeds.\r\n- Start the backend with SESSION_SECRET and GOOGLE_CLIENT_ID unset and confirm it refuses to start.\r\n- `Select-String -Path backend\\*.py,src\\*.jsx,src\\*.js -Pattern \"google_token_|_decode_jwt_payload_fallback|local-fallback-token\" -Recurse` returns nothing.\r\n\r\nFINAL REPORT\r\nFor each task 1-12: files changed, what changed, PASS/FAIL with evidence. List anything you could not do or that surprised you.\n</USER_REQUEST>\n<ADDITIONAL_METADATA>\nThe current local time is: 2026-09-20T15:01:48+05:30.\n\nThe user's current state is as follows:\nActive Document: c:\\Users\\Devendra\\Desktop\\SIH\\backend\\.env (LANGUAGE_UNSPECIFIED)\nCursor is on line: 1\nOther open documents:\n- c:\\Users\\Devendra\\Desktop\\SIH\\AUDIT.md (LANGUAGE_MARKDOWN)\n- c:\\Users\\Devendra\\Desktop\\SIH\\backend\\.env (LANGUAGE_UNSPECIFIED)\nBrowser State:\n  Page E408248FA8274FA51C065A15A9222579 (Vantage widget) - https://vantage.csw.lenovo.com/v1/web/containerwidget/default/ [ACTIVE]\n    Viewport: 64x64, Page Height: 64\n</ADDITIONAL_METADATA>"}
